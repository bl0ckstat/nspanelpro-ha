# Copy to configs/secrets.env (gitignored) and fill in.
# deploy-subnet.sh sources this and uses it in place of the placeholder in
# each configs/<ip>.yaml, so credentials never enter the repository.
MQTT_PASSWORD=""
